.full-height {
  height: 100%;
}

html, body, #__nuxt {
  margin: 0;
  padding: 0;
  @extend .full-height;
}

.wrapper {
   @extend .full-height;
}

.content-wrapper {
  height: calc(100vh - 56px);
}

.flex-grow {
  flex: 1;
}